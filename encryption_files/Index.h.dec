#ifndef INDEX_H
#define INDEX_H

#include <unordered_map>
#include <vector>
#include "Log.h"

class Index {
public:
   std::unordered_map<std::string, std::vector<int>> mentions;
   std::unordered_map<std::string, unsigned int> counts;
   Index(Log const &log);
   void search_message(std::vector<std::string> keywords,
                       std::vector<int> &matches);
};


#endif//INDEX_H
